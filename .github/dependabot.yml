version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
    groups:
      # Bundle related updates into fewer PRs to reduce noise.
      all-dependencies:
        patterns:
          - "*"
